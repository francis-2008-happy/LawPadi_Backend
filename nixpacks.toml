[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
# Optionally build index during deployment (may take 5-10 minutes)
# Uncomment the line below to auto-build index on deployment:
# cmds = ["python -m app.rag.build_index"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
